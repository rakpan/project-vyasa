# Project Vyasa - Environment Template
# Copy this file to deploy/.env and fill in real values.

# -----------------------------
# Service Images
# -----------------------------
CORTEX_IMAGE=nvcr.io/nvidia/sglang:latest
DRAFTER_IMAGE=ollama/ollama:latest
MEMORY_IMAGE=arangodb:latest
VECTOR_IMAGE=qdrant/qdrant:latest
ORCHESTRATOR_IMAGE=vyasa-orchestrator:latest

# -----------------------------
# Ports (host:container)
# -----------------------------
PORT_BRAIN=30000
PORT_WORKER=30001
PORT_VISION=30002
PORT_DRAFTER=11434
PORT_CONSOLE=3000
PORT_MEMORY=8529
PORT_VECTOR=6333
PORT_EMBEDDER=8000

# -----------------------------
# Model Paths / Tokens
# -----------------------------
BRAIN_MODEL_PATH=/models/brain
WORKER_MODEL_PATH=/models/worker
VISION_MODEL_PATH=/models/vision
BRAIN_MODEL_NAME=meta-llama/Llama-3.3-70B-Instruct
WORKER_MODEL_NAME=Qwen/Qwen2.5-32B-Instruct
VISION_MODEL_NAME=Qwen/Qwen2-VL-72B-Instruct
HF_TOKEN=changeme   # Required if pulling gated models

# -----------------------------
# GPU Binding (use comma-separated IDs, or leave empty for CPU-only where supported)
# -----------------------------
BRAIN_GPU_IDS=0
WORKER_GPU_IDS=1
VISION_GPU_IDS=2
DRAFTER_GPU_IDS=3

# -----------------------------
# Network / Containers
# -----------------------------
NETWORK_NAME=vyasa-net
CONTAINER_CONSOLE=vyasa-console
CONTAINER_MEMORY=vyasa-memory
CONTAINER_QDRANT=vyasa-vector
CONTAINER_EMBEDDER=vyasa-embedder
CONTAINER_ORCHESTRATOR=vyasa-orchestrator

# -----------------------------
# Database / Auth
# -----------------------------
GRAPH_DB_NAME=project_vyasa
ARANGODB_USER=root
ARANGODB_PASSWORD=
ARANGO_ROOT_PASSWORD=
QDRANT_API_KEY=
CONSOLE_PASSWORD=changeme
CONSOLE_SECRET=changeme
NEXTAUTH_SECRET=changeme

# -----------------------------
# Vector / Embedder
# -----------------------------
EMBEDDER_MODEL_NAME=all-MiniLM-L6-v2

# -----------------------------
# Vision Controls
# -----------------------------
VISION_MAX_IMAGES=5

# -----------------------------
# Orchestrator
# -----------------------------
ORCHESTRATOR_URL=http://orchestrator:8000
