[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:importlib.*
    ignore::DeprecationWarning:swigvarlink
markers =
    integration: marks tests as integration tests (requiring DB/Docker)
